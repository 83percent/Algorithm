"use strict";
(() => {
    function solution(word) {
        let result = 0;
        const alphabet = ['X', 'A', 'E', 'I', 'O', 'U'];
        function combi() {
        }
        return result;
    }
    solution('AAAAE');
})();
